server {
    listen 80;

    location / {
        root   /usr/share/nginx/html;
        index  index.html;
        # Fallback to index.html for client-side routing
        try_files $uri $uri/ /index.html;
    }
}
